import 'package:flutter/material.dart';

class PaginaCrear extends StatelessWidget {
  const PaginaCrear({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Pagina Crear"),
    );
  }
}
